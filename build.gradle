buildscript {
    repositories {
        google()
        jcenter() // Use jcenter if required by your dependencies
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.0.0'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
